name: infinite_list_bloc
description: A new Flutter project

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^4.0.0
  http: ^0.12.0
  equatable: ^1.0.0
  rxdart: ^0.23.1

  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true